/*Escribe un programa que pida un número y 
muestre ese número al cuadrado y al cubo (multiplicado por sí mismo dos y tres veces). 
Si el número es menor que 1, no calcules el cubo ni el cuadrado.*/

numstring = parseInt (window.prompt ('Escribe un numero'));

if (numstring < 1 ) {
    console.log ( "no se calcula")
}
else (Math.pow(numstring,2) )
{
    console.log (Math.pow(numstring,2))
}